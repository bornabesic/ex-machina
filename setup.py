
from setuptools import setup, find_packages

setup(
    name="ex-machina",
    author="Borna Bešić",
    description="Access the state of a running program via socket-based REPL",
    packages=find_packages(),
)
